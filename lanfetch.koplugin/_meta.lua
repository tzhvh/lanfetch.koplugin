local _ = require("gettext")

return {
    fullname = _("LAN PDF Downloader"),
    description = _([[Download files served on the local network (LAN) by bare IP and port — PDFs, EPUBs, HTML, Markdown, ZIP archives and more — with an e-ink optimized keypad, subnet autodetection, hierarchical folder presets, optional archive unzipping, and ephemeral URL state.]]),
    category = "network",
    version = "1.1",
}
